<div class ="genre-edit">
	<h1>ジャンル編集画面</h1>



     <%= form_for(@genre,url: {controler: "genres", action: "update" }) do |f| %>
     	<%= f.text_field :name %><%= f.submit "変更を保存する" %>
     	<%= f.radio_button :is_active, :true %>有効<%= f.radio_button :is_active, :true %>無効
     <% end %>















</div>