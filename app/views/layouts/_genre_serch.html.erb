<div class="col-2">
  <table class="table-borderless">
    <thead>
      <tr>
        <th scope="col">Category</th>
      </tr>
    </thead>
        <tbody>
            <% @genres.each do |genre| %>
                <tr>
                  <td scope="row" class="borderless"><%= link_to "#{genre.name}", controller: "items", action: "index", genre_id: genre.id %></td>
                </tr>
            <% end %>
        </tbody>
  </table>
</div>